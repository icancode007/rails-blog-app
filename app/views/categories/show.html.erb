<h1 align="center"><%= @category.name%> Article's </h1>
<%if logged_in? and current_user.admin? %>
  <div align="center">
    <%= link_to "edit", edit_category_path(@category)%>
  </div>
<% end %>
<div align="center">
  <p><%= render '/articles/article', obj:@category_articles %></p>

</div>
